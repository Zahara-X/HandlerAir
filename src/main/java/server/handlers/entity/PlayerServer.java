package server.handlers.entity;

public class PlayerServer extends ArrayEntityServer {
    public PlayerServer(int x, int y, int width, int height) {
        super(x, y, width, height);
    }
    public PlayerServer() {}
}